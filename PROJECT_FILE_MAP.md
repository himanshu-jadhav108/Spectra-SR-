# Project File Map

This package is a specification bundle rather than the finished Spectra SR application.

`docs/` = what the application must do
`prompts/` = how to ask Antigravity to implement it
`contracts/` = machine-readable API contract
`config/` = environment contract
`scaffold/` = minimal code boundaries to reduce ambiguity
`tests/` = acceptance criteria
